# Magnetic Fields — biome music

Drop one or more looping `.ogg` tracks for the Magnetic Fields biome here.
The game auto-plays files in this folder when the player is in the biome
(music manifest key `music.magnetic_fields`, see src/systems/sound/soundDefaults.ts).
